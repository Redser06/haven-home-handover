# ADR-002: Key-safe Encryption

## Decision
Key-safe secrets will be encrypted client-side using a per-handover key derived and held only on participant devices.

## Context
We need to ensure that the server only stores ciphertext.
